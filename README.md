Implemented createAsyncThunk middleware from Redux toolkit for managing asynchronous CRUD operations,
handling API calls and dispatching lifecycle actions.

• Leveraged extraReducers in Redux slice for responding to API calls and updating loading, error and data state in
a centralised manner.

• Integrated React Router for connecting and rendering components. Managed component-level state with global
Redux state for consistent data flow with backend API data. experience.
